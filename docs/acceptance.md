# 首版验收记录

日期：2026-09-16。状态：实现与离线验证阶段，待微信端联调。

## 本地验证

- Node.js 24.19.0、pnpm 11.19.0 环境安装固定依赖。
- TypeScript / Vue 模板类型检查。
- 微信小程序生产构建，目录 `dist/build/mp-weixin`。
- 微信原生 WXSS 编译器检查：已复现并修复 `.stack > * + *` 通配符导致的 `error at token '*'`；修复后全部 6 个生成样式文件编译通过。运行 `pnpm check:wxss` 可复查，仍不等于完整真机验收。
- 离线测试：URL 与凭证边界、Cookie 提取/有效期/删除、缓存隔离、旧响应拒绝、业务信封、上传会话、精确金额、模型组合、生成入参、批次部分失败、运行时任务上限、文本去重、安全日志和结果。

测试不调用真实模型、不注册账号、不发送邮件、不兑换积分。没有启动开发服务，也没有远端推送或发布。

## 待开发者工具和真机验证

先配置真实兼容后端与微信 AppID；真实写入需明确测试账号、模型额度和兑换码范围。

- [ ] 配置提示、冷启动、未登录跳转、三 Tab、安全区与键盘。
- [ ] 用户名/邮箱登录、全部注册字段、首用户、注册关闭、SMTP 不可用。
- [ ] 验证码限流/过期、后台倒计时、密码找回及旧会话失效。
- [ ] Cookie 接收、上传携带、到期、重启恢复、退出失败提示。
- [ ] 切账号/实例不闪现旧数据，迟到请求不覆盖新会话。
- [ ] 两种模型目录、禁用/空目录、参数组合、上传上限。
- [ ] 文本/图片/视频真实生成、文本继续提问、多图部分失败。
- [ ] 前后台及切页恢复、弱网/限流、取消与账务状态。
- [ ] 图片预览、视频播放、签名刷新、相册权限、保存与空间不足。
- [ ] 最近 100 条边界、跨 Web 任务、未知类型、筛选范围。
- [ ] 兑换成功/失败/结果未知，金额与 Web 一致，分页/分类切换。
- [ ] iOS/Android 微信布局、长描述、长结果、大字体。

通过后记录：后端完整 commit、开发者工具及基础库版本、机型/系统、时间和实际结果。构建成功不等于真机闭环通过。
