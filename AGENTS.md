# AGENTS.md

本文件适用于本仓库内的 AI 编程代理、自动化工具和协作者。用户当前任务优先于本文件。所有实现与结论应能追溯到源码、接口响应、配置或验证结果。

## 1. 项目定位和首版边界

本项目是 [Open AI Canvas](https://github.com/ddcat-ai/open-ai-canvas) 的独立 uni-app 客户端，使用 Vue 3、TypeScript、Pinia，支持微信小程序和 Android/iOS App 资源构建，复用现有后端，不修改其业务接口。

当前已有页面、业务接口和平台适配实现。平台资源构建及离线测试通过不代表真机验收通过；修改前先检查实际代码。

首版只包含：

- 账号：用户名/邮箱与密码登录、完整注册表单、邮箱验证码、密码找回、会话恢复、退出。
- 创作首页：视频、图片、文本生成；系统模型选择；模型能力驱动的参数；图片/视频/音频参考上传并入库；结果预览、复制或保存。
- 任务：最近任务、筛选、详情、进度、日志、结果、取消和符合条件的重试。
- 积分：可用余额、预占积分说明、兑换码兑换、分页积分流水。

底部 Tab 为“创作 / 任务 / 积分”。账号菜单放在首页，认证页面独立于 Tab。

未经用户明确扩展，不加入微信一键登录、第三方 OAuth、在线支付、充值套餐、签到入口、音频生成、素材库独立页、项目/分镜、画布、Agent、剪辑、插件或管理后台。

## 2. 开工前

1. 阅读 README、当前任务涉及的代码、接口调用方和相邻测试。
2. 执行 `git status --short`，保留用户已有改动；不进行无关重构或格式化。
3. 新功能先查后端和现有 Web 实现，不能仅根据接口名字猜测行为。
4. 优先使用 `rg` 搜索。手工文件编辑使用 `apply_patch` 或环境提供的结构化编辑工具。
5. 未经请求，不启动服务、运行付费生成、消耗兑换码或创建真实业务数据。只读检查和本地离线验证可正常进行。

参考后端基线为 `ddcat-ai/open-ai-canvas` 的 `9868f5e8`。它是静态分析基线，不是已经验证的兼容版本。后续真机联调后记录实际通过的提交或版本，不宣称永久兼容上游 main。

## 3. 后端地址必须只改一处

工程实现后，唯一业务后端配置为 `src/config/backend.ts`：

```ts
export const backendConfig = Object.freeze({
  apiBaseUrl: 'https://your-domain.example/api',
});
```

- API 根地址包含 `/api`，不带尾部斜杠。仅允许真实部署使用 HTTPS。
- 页面和业务 API 使用 `/auth/login`、`/tasks` 等相对路径。
- 普通请求、上传、下载、签名地址请求及可选流式传输共享该配置。
- 不在其他源码中写部署域名，不增加 `.env` 地址覆盖、隐式默认服务器或第二个 baseURL。
- README 和配置示例可包含明确的占位域名，不得把个人服务器作为默认公共服务。
- 未配置时显示配置错误，不静默连接演示站。
- 更换兼容后端只改配置并重新编译。AppID、微信合法域名、OSS/CDN 域名和后端管理配置属于独立的部署前提，文档不得混淆。
- 若后端接口合同变化，修改对应 API/适配模块；不得承诺不同接口后端也只需换 URL。

## 4. 分层与目录职责

推荐结构如下，按实际功能逐步创建，禁止为了目录齐全而添加空壳封装。

| 目录                           | 职责                                                |
| ------------------------------ | --------------------------------------------------- |
| `src/config/`                  | 唯一后端地址配置及明确的应用配置                    |
| `src/core/http.ts`             | 请求、统一业务信封、错误与取消语义                  |
| `src/core/session.ts`          | Cookie 解析、携带、失效与清理                       |
| `src/core/storage.ts`          | 后端实例与账号隔离的存储                            |
| `src/core/urls.ts`             | API 和资源地址解析                                  |
| `src/api/`                     | auth、models、tasks、resources、wallet 的接口和 DTO |
| `src/adapters/open-ai-canvas/` | 生成入参、结果和模型能力的后端特有映射              |
| `src/services/`                | 上传、生成编排、共享任务轮询、媒体保存              |
| `src/stores/`                  | 跨页面状态和缓存失效                                |
| `src/components/`              | 真实复用的表单、任务卡片、模型参数与媒体组件        |
| `src/pages/`                   | 页面布局、交互、状态展示                            |
| `src/styles/`                  | 集中设计变量与基础样式                              |
| `docs/`                        | 部署、接口兼容、验证说明                            |

依赖方向为页面 → store/service → API/适配器 → core → 后端。页面不得直接调用 `uni.request`、解析 Cookie、解析 `resultJson` 或自行计算最终账务。

使用 TypeScript 明确声明输入、输出和错误类型。不要直接复制 React、Ant Design、DOM、IndexedDB、localForage 依赖到小程序。业务纯函数只有在依赖与许可明确时才复用。

## 5. 账号合同

- 登录必须支持“用户名 / 邮箱”，请求 `{ username, password }`；邮箱也放在 `username` 字段。
- 注册保留用户名、显示名称、邮箱、邮箱验证码、密码、确认密码。
- 用户名为 3–32 位字母、数字、下划线或连字符；显示名称选填；密码至少 8 位。确认密码只作前端校验。
- 不用邮箱充当用户名，不静默生成用户名来省略表单字段。
- 从 `/auth/settings` 读取注册、邮件、验证码和首用户规则，不写死开放注册。
- 找回密码沿用“发送邮箱验证码 → 设置新密码”，发送提示不暴露账号是否存在。
- 登录/注册成功接收会话，再读取 `/auth/session`。HTTP 200 且 `user: null` 代表未登录。
- 重置密码后清理本地会话并返回登录。退出时清理当前用户状态、缓存和订阅；注销请求失败时不得声称服务器会话已撤销。

认证视觉参照现有 Web：深色卡片、蓝灰英文眉题、白色主标题与按钮、登录/注册切换下划线、密码显隐。小屏表单采用单列，不删除原有字段。登录标题为“进入创作现场”，注册标题为“建立你的创作空间”。

## 6. 请求、凭证和资源

- 统一识别 HTTP 错误与 `{ code, data, msg, reason }`；不能把 HTTP 200 一律视为成功。
- 登录 Cookie 名为 `open_ai_canvas_session`。从响应 `cookies` 或 `Set-Cookie` 解析，处理多 Cookie、属性、有效期和删除，不原样转发整个 Set-Cookie。
- API 请求和上传使用同一会话管理。Cookie 只发给受信任 API，不能附带到媒体签名 URL、第三方域名或模型供应商。
- 会话按完整 API 根地址隔离；用户缓存按“后端实例 + 用户 ID”隔离。
- 切换账号或实例时取消旧请求、停止轮询并丢弃迟到响应。
- 密码、验证码、Cookie、供应商密钥、签名 URL 不进入日志或公开诊断输出。
- 上传通过 `uni.uploadFile`，文件字段 `file`；上传响应字符串仍须统一解包。
- 不手动构造上传 multipart boundary。
- 优先使用 `/resources/:id/oss-url` 获取短期媒体地址；过期刷新。区分绝对、根相对和 API 相对路径，避免重复 `/api`。
- 超时不是“操作未发生”。生成、兑换、注册等写操作禁止通用自动重试。

## 7. 生成与任务

- 使用后端系统模型目录与逻辑模型，前端不保存供应商密钥，不提供自定义渠道首版入口。
- 根据模型能力显示参数、参考图数量和操作类型；换模型时重新校验，不提交不支持的选项。
- 任务入参和结果解析参考上游 `web/src/services/api/generation-task.ts`，区分 canvas_text、canvas_image、canvas_video 及 operation。
- 图片/视频/音频参考先上传再传资源引用，不提交手机临时路径或内嵌媒体。上传后单条写入同账号个人素材库，失败仅重试入库、不可重传文件；待同步记录按账号隔离，不保存临时路径或签名 URL。移除参考不删除素材。参考视频/音频入口和数量遵循模型能力，不增加音频生成。
- 收到任务 ID 立即保存；批量部分成功时记录成功项，不整批重发。
- `clientOperationId` 不能未经验证就视为服务端幂等保障。
- 文本首版使用 `text-deltas?after=`，按 sequence 去重并处理最终文本；不为此新增后端 WebSocket。
- 任务轮询统一调度；页面隐藏或应用后台时暂停客户端查询，回前台恢复，不自动取消服务端任务。
- 取消、上游取消确认和退款不是同一状态；提示必须以真实返回值为准。
- 任务列表最多返回最近 100 条，无服务端历史分页。只在已加载集合筛选和统计，不能制造无限历史加载。
- Web 产生的未知任务类型应安全展示基础信息，不误解析为首版支持类型。
- 小程序成功任务自动写入主项目个人 `/assets` 素材库，生成状态与素材同步状态分开。使用 Web 的 `generation_ + SHA256(materialize:taskId:outputIndex)` ID，先读取已有记录再单条 PUT；不得以全库替换实现同步。后台完成任务须读取详情后补同步，失败提供重试并保留成功结果。此关联不包含 Web 会话分组、画布或项目资产。

## 8. 积分

- 使用 GET `/wallet` 的真实分页与分类，POST `/wallet/redeem` 提交 `{ code }`。
- 余额分清可用与预占；`1 积分 = 1,000,000 microcredits`。内部保留整数，展示时格式化。
- 不在前端决定最终扣费；预估必须标注，实际消耗以服务端账务为准。
- 创建任务、任务结算状态变化、兑换成功及进入积分页时刷新余额。
- 兑换期间防重复点击；提交结果未知先核对余额和流水，不自动重发。
- 流水分类沿用 all、income、consume、refund；即使不提供支付或签到入口，也不能隐藏已经发生的相关流水。

## 9. 验证与交付

App 平台差异集中于 `src/core/runtime.ts` 和原生适配模块。新增原生 API 调用须保留微信路径；Android/iOS 音频选择不能调用微信的 `chooseMessageFile`。App 任务使用 `uniapp-app` 来源，素材同步须兼容既有 `uniapp-wechat` 任务。运行步骤和真机验收见 `docs/app.md`。`pnpm build:app` 仅编译资源，不等于签名 APK/IPA 或上架。

按变更风险选择最小充分验证。文档变更检查路径、链接、命令和前后表述；工程建立后再使用真实存在的 typecheck、构建和测试脚本，不报告未执行的命令通过。

关键逻辑值得验证：URL 解析、Cookie 提取与隔离、业务错误解包、生成 payload/结果映射、文本游标去重、金额格式化和迟到响应处理。不要只写镜像实现的测试。

微信开发者工具及 iOS/Android 微信真机至少验证账号、上传、三类生成、后台恢复、媒体预览、兑换和积分一致性。H5 构建通过不能代替小程序验证。真实写入和计费验收先明确测试账号、额度及兑换码范围。

交付说明必须区分：已实现、已验证、未验证、已知限制。接口或配置变更同步 README 和相关 docs；新增功能不得只留下不可见的实现。

当前仓库许可证与上游复制代码的版权声明需保留；新增第三方依赖记录来源及许可。不得提交凭证、node_modules、构建产物或开发者工具私有配置。

默认使用一个包管理器与锁文件；搭建工程时确认选择并更新 README，不混用锁文件。除非明确授权，不推送、发布或修改远端设置。推送时不强推共享主线，遇到新提交先检查和整合。
